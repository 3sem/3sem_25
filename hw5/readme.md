## Взаимодействие между процессами, синхронизированное сигналами

### Запуск

По команде 
```
make run
```
будут скомпилированы исполняемые файла программы-отправителя и -получателя. Затем создастся файла в 4 Гб под названием input_file.txt с рандомизированным содержимым. Этот файл обработается программой, результат записан в файл output.txt. Затем проверяются md5 суммы, выводятся они и сообщение-результат.
По команде
```
make clean
```
все созданные файлы будут удалены.

### Работа

Передача через разделяемую память. Пиды процессов записываются там же. для надежности блокируются все сигналы, кроме тех, что используются в работе - SIGUSR1 (отправляется отправителем), SIGUSR2 (отправляется получателем). Поэтому программа надежна, но если что-то случится, отключать из терминала kill -9.

### Анализ данных

Передано 5 раз, среднее время - 12,4 сек.